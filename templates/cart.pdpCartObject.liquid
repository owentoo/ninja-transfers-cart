{%-liquid
  layout none
-%}

{% if cart.item_count > 0 %}
  {%- capture getCartData -%}
    {
      "original_total_price": {{ cart.original_total_price | divided_by: 100 | json }}
    }
  {%- endcapture -%}

  {{ getCartData }}
{% else %}
  {
    "original_total_price": 0
  }
{% endif %}
